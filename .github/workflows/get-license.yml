# Unity workflows disabled — project migrated to Godot 4
# See .github/workflows/build-android.yml for active build
name: Unity License (DISABLED)
on:
  workflow_dispatch:
    inputs:
      disabled:
        description: 'This workflow is disabled'
        required: false
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Unity workflows are disabled. Project uses Godot 4."
